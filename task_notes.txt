A. build home page account
	add home template, with dummy search bar and button on the footer
	nav stack

B. the tab nav bar should have home/account
	if user is logged in, the account view should render logout page
	if not, render the login form
		add user auth system in the backend

C. add re
